services:
  - type: redis
    name: cnc-pwa-redis
    region: singapore
    plan: free
    maxmemoryPolicy: allkeys-lfu
    ipAllowList:
      - source: 0.0.0.0/0
        description: Allow all

  - type: web
    name: cnc-pwa
    env: python
    region: singapore
    buildCommand: './build.sh'
    startCommand: 'daphne -b 0.0.0.0 -p $PORT --proxy-headers cybernetcall.asgi:application'

    plan: free
    branch: main
    healthCheckPath: /
    envVars:
      - key: DJANGO_DEBUG
        value: "false"
      - key: PYTHONUNBUFFERED
        value: "true"
      # DATABASE_URL はRenderのダッシュボードでSecretとして設定してください。
      # Supabaseプロジェクトの接続文字列（postgres://...）を使用します。
      - key: REDIS_URL
        fromService:
          type: redis
          name: cnc-pwa-redis
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: "1"
    autoDeploy: true
